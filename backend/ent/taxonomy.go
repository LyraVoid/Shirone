// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/shirone-platform/backend/ent/taxonomy"
)

// Taxonomy is the model entity for the Taxonomy schema.
type Taxonomy struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Key holds the value of the "key" field.
	Key string `json:"key,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TaxonomyQuery when eager-loading is set.
	Edges        TaxonomyEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TaxonomyEdges holds the relations/edges for other nodes in the graph.
type TaxonomyEdges struct {
	// Terms holds the value of the terms edge.
	Terms []*Term `json:"terms,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TermsOrErr returns the Terms value or an error if the edge
// was not loaded in eager-loading.
func (e TaxonomyEdges) TermsOrErr() ([]*Term, error) {
	if e.loadedTypes[0] {
		return e.Terms, nil
	}
	return nil, &NotLoadedError{edge: "terms"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Taxonomy) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case taxonomy.FieldID:
			values[i] = new(sql.NullInt64)
		case taxonomy.FieldKey, taxonomy.FieldName, taxonomy.FieldDescription:
			values[i] = new(sql.NullString)
		case taxonomy.FieldCreatedAt, taxonomy.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Taxonomy fields.
func (_m *Taxonomy) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case taxonomy.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case taxonomy.FieldKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field key", values[i])
			} else if value.Valid {
				_m.Key = value.String
			}
		case taxonomy.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case taxonomy.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		case taxonomy.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case taxonomy.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Taxonomy.
// This includes values selected through modifiers, order, etc.
func (_m *Taxonomy) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTerms queries the "terms" edge of the Taxonomy entity.
func (_m *Taxonomy) QueryTerms() *TermQuery {
	return NewTaxonomyClient(_m.config).QueryTerms(_m)
}

// Update returns a builder for updating this Taxonomy.
// Note that you need to call Taxonomy.Unwrap() before calling this method if this Taxonomy
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Taxonomy) Update() *TaxonomyUpdateOne {
	return NewTaxonomyClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Taxonomy entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Taxonomy) Unwrap() *Taxonomy {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Taxonomy is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Taxonomy) String() string {
	var builder strings.Builder
	builder.WriteString("Taxonomy(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("key=")
	builder.WriteString(_m.Key)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Taxonomies is a parsable slice of Taxonomy.
type Taxonomies []*Taxonomy
